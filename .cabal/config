-- This is the configuration file for the 'cabal' command line tool.

-- The available configuration options are listed below.
-- Some of them have default values listed.

-- Lines (like this one) beginning with '--' are comments.
-- Be careful with spaces and indentation because they are
-- used to indicate layout for nested sections.


remote-repo: hackage.haskell.org:http://hackage.haskell.org/packages/archive
remote-repo-cache: /home/bairyn/.cabal/packages
-- local-repo:
-- logs-dir:
world-file: /home/bairyn/.cabal/world
-- verbose: 1
-- compiler: ghc
-- with-compiler:
-- with-hc-pkg:
-- scratchdir:
-- program-prefix: 
-- program-suffix: 
-- library-vanilla: True
library-vanilla: True
-- library-profiling: False
library-profiling: True
-- shared: False
shared: True
-- executable-dynamic: False
--executable-dynamic: True
-- ^ This *just doesn't work*, it seems.
-- executable-profiling: False
-- optimization: True
-- library-for-ghci: True
library-for-ghci: True
-- split-objs: False
-- executable-stripping: True
-- user-install: True
-- package-db:
-- flags:
-- extra-include-dirs:
-- extra-lib-dirs:
-- constraint:
-- tests: False
tests: True
-- library-coverage: False
-- benchmarks: False
-- cabal-lib-version:
-- preference:
-- documentation: False
documentation: True
-- doc-index-file: $datadir/doc/index.html
-- upgrade-dependencies:
-- only-dependencies:
-- root-cmd:
-- symlink-bindir:
build-summary: /home/bairyn/.cabal/logs/build.log
-- build-log:
remote-build-reporting: anonymous
-- one-shot:
-- username:
-- password:
-- username:
-- password:

install-dirs user
  -- prefix: /home/bairyn/.cabal
  -- bindir: $prefix/bin
  -- libdir: $prefix/lib
  -- libsubdir: $pkgid/$compiler
  -- libexecdir: $prefix/libexec
  -- datadir: $prefix/share
  -- datasubdir: $pkgid
  -- docdir: $datadir/doc/$pkgid
  -- htmldir: $docdir/html
  -- haddockdir: $htmldir

install-dirs global
  -- prefix: /usr/local
  -- bindir: $prefix/bin
  -- libdir: $prefix/lib
  -- libsubdir: $pkgid/$compiler
  -- libexecdir: $prefix/libexec
  -- datadir: $prefix/share
  -- datasubdir: $pkgid
  -- docdir: $datadir/doc/$pkgid
  -- htmldir: $docdir/html
  -- haddockdir: $htmldir
